# Ambiguous
Input: `Make popular URLs faster.`
Requirement analysis surfaces missing measurable definitions. A human can re-plan with a clarified requirement such as `Cache URLs after 100 redirects in 10 minutes; p95 redirect latency under 50ms`. The orchestrator invalidates dependent stages, preserves prior audit history and requires renewed design approval.
