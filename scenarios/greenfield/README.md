# Greenfield
Input: `Build a production-ready URL shortener with analytics and expiration.`
The run normalizes requirements, decomposes work, fans out architecture/security in parallel, synchronizes at a human design gate, implements, fans out test/review, validates, then waits for human release approval.
