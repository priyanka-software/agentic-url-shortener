# Brownfield
Input: `Add expiration and analytics to the existing URL shortener.`
Architecture output identifies impacted modules (controller, service, entity, repository, tests and docs). For the resilience demo, set `simulateTestFailure:true`; tests retry only within the configured budget and then SAFE_STOP, blocking release.
